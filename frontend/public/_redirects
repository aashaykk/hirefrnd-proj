# Redirect rules for Netlify
# 1. Proxy api requests to the deployed backend service (replace with your actual backend service URL)
/api/*    https://your-backend-service.onrender.com/api/:splat    200

# 2. Redirect all other requests to index.html for React Router client-side routing
/*        /index.html                                             200
